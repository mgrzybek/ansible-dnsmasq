---

- name: Check systemd-resolvd
  shell: systemctl is-active systemd-resolved.service || echo ko
  register: resolvd_result

- set_fact:
    disable_resolvd: false

- when: resolvd_result.stdout.find('ko') < 0
  set_fact:
    disable_resolvd: true
